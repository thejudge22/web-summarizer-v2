services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - SUMMARY_DATA_DIR=/app/data
    env_file:
      - .env
